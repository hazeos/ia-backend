import { HidePropertiesInterceptor } from './hide-properties.interceptor';

describe('HidePropertiesInterceptor', () => {
  it('should be defined', () => {
    expect(new HidePropertiesInterceptor()).toBeDefined();
  });
});
